# Web framework
fastapi>=0.110
uvicorn[standard]>=0.24

# Multipart uploads (required by FastAPI for UploadFile/Form)
python-multipart>=0.0.9

# HTTP client (used for integrations/tests; keep in runtime if server uses it)
httpx>=0.28.0

# Paths / platform
platformdirs>=4.0

# Data / validation
jsonschema>=4.0

# Numeric / vector search
numpy>=1.26
hnswlib>=0.8.0
onnxruntime>=1.17.0

# Crypto / signing
cryptography>=42.0

# Document text extraction
pypdf>=4.0.0
python-docx>=1.1.0
